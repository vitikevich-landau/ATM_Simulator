# Нормализуем переводы строк: в репозитории всегда LF.
# Это важно, потому что разработка идёт на Windows, а сборка/проверка — в Linux-контейнере.
* text=auto eol=lf

# Бинарные файлы не трогаем.
*.png binary
*.jpg binary
*.pdf binary
