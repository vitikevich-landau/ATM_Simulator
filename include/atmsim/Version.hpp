#pragma once
// ============================================================================
//  Version.hpp — метаданные проекта в одном месте.
//
//  Держим имя и версию здесь, чтобы не «зашивать» строки в разных файлах.
//  constexpr означает, что значения известны на этапе компиляции и не занимают
//  место как обычные глобальные переменные.
// ============================================================================

namespace atmsim {

// Человекочитаемое имя проекта (для баннера, логов, отчётов).
inline constexpr const char* kProjectName = "ATM Simulator";

// Версия. Должна совпадать с project(... VERSION ...) в CMakeLists.txt.
inline constexpr const char* kVersion = "0.1.0";

}  // namespace atmsim
