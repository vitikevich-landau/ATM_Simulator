// ============================================================================
//  main.cpp — точка входа консольного симулятора банкомата (ATM Queue Simulator).
//
//  Этап M0 (каркас): здесь пока только стартовый баннер. Это сознательно —
//  цель M0 в том, чтобы проект гарантированно собирался и запускался на обеих
//  целевых платформах (Windows 10+ и Linux), прежде чем мы начнём наращивать
//  логику. Дальше по дорожной карте (см. README.md):
//    M1 — доменное ядро (Money, Account, Cashbox, Client) + юнит-тесты;
//    M2 — однопоточная симуляция очереди;
//    M3 — многопоточность (jthread, прерываемое ожидание) — самая важная часть;
//    M4 — отчётность; M5 — техобслуживание; M6 — живой дашборд; M7 — полировка.
// ============================================================================

#include <iostream>

#include "atmsim/Version.hpp"

int main() {
    // На этом этапе просто подтверждаем, что сборка и запуск работают.
    std::cout << atmsim::kProjectName << " v" << atmsim::kVersion << '\n';
    std::cout << "Консольный симулятор работы банкомата (одноканальная СМО).\n";
    std::cout << "Этап M0: каркас проекта. Доменное ядро ещё не реализовано.\n";
    return 0;
}
